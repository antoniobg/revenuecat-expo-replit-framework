# RevenueCat Configuration
# Copy this file to .env.local and fill in your actual values

# RevenueCat API Keys
# Get these from: https://app.revenuecat.com/projects/[your-project]/api-keys
EXPO_PUBLIC_RC_IOS_API_KEY=appl_xxxxxxxxxx
EXPO_PUBLIC_RC_ANDROID_API_KEY=goog_xxxxxxxxxx
EXPO_PUBLIC_RC_WEB_API_KEY=strp_xxxxxxxxxx

# RevenueCat Configuration
# Set this to match your entitlement identifier in the RevenueCat dashboard
EXPO_PUBLIC_RC_ENTITLEMENT_ID=premium

# Optional: Environment Configuration
# Set to 'true' to enable debug mode (defaults to development mode)
EXPO_PUBLIC_RC_DEBUG_MODE=true

# Optional: User Identification
# Set to 'true' if you want to use custom user IDs instead of anonymous IDs
EXPO_PUBLIC_RC_USE_USER_IDENTIFICATION=false

# Development Notes:
# - These are public environment variables (EXPO_PUBLIC_) safe for client-side use
# - RevenueCat API keys are designed to be used in client applications
# - Never put secret keys or server-side credentials in environment variables
# - The .env.local file should not be committed to version control

# Platform-Specific Setup:
# iOS: Configure products in App Store Connect and link in RevenueCat dashboard
# Android: Configure products in Google Play Console and link in RevenueCat dashboard
# Web: Configure your web products on the RevenueCat dashboard
